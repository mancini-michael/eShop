<div class="modal fade" id="insertion" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg">
    <div class="modal-content">
      <%# modal header %>
      <div class="modal-header">
        <button class="btn-close position-absolute m-0" data-bs-dismiss="modal"></button>
        <div class="d-flex flex-column align-items-center justify-content-center w-100">
          <h5 class="modal-title fw-bold mx-auto">Vendi</h5>
        </div>
      </div>

      <%# modal body %>
      <div class="modal-body py-3">
        <div class="mb-3">
          <h5 class="mb-0">Vendi su Commerce</h5>
        </div>
        <%= form_for(:insertion, url: insertion_path) do |f| %>
          <%= f.hidden_field :seller_id, value: Seller.find_by(user_id: @user).id %>
          <div class="row">
            <div class="col-9">
              <div class="form-floating mb-3">
                <%= f.text_field :title, class: "form-control", placeholder: "title" %>
                <%= f.label :title %>
              </div>
            </div>
            <div class="col-3">
              <div class="form-floating">
                <%= f.number_field :price, class: "form-control", placeholder: "price" %>
                <%= f.label :price %>
              </div>
            </div>
          </div>
          <div class="form-floating mb-3">
            <%= f.text_area :description, class: "form-control", placeholder: "description", style: "height: 400px;" %>
            <%= f.label :description %>
          </div>
          <div class="row mb-3">
            <div class="col">          
              <%= f.select :categories, Insertion.categories, {}, { class: "form-select" } %>
            </div>
            <div class="col">
              <%= f.file_field :image %>
            </div>
          </div>
          <%= f.submit "Continua", class: "modal-btn" %>
        <% end %>
      </div>
    </div>
  </div>
</div>